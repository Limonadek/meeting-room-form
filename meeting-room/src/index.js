import React from 'react';
import ReactDOM from 'react-dom/client';
import MeetingRoomForm from './MeetingRoomForm.js';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <MeetingRoomForm />
  </React.StrictMode>
);
